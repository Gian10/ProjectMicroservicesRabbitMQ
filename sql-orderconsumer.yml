apiVersion: apps/v1
kind: Deployment
metadata:
  name: sql-orderconsumer-deployment
  labels:
    app: sql-orderconsumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sql-orderconsumer
  template:
    metadata:
      labels:
        app: sql-orderconsumer
    spec:
      containers:
      - name: sql-orderconsumer
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
        - name: ACCEPT_EULA
          value: "Y"
        - name: MSSQL_SA_PASSWORD
          value: "SqlServer2025!"
        - name: MSSQL_PID
          value: "Developer"
        ports:
        - containerPort: 1433
---
apiVersion: v1
kind: Service
metadata:
  name: sql-orderconsumer-service
spec:
  selector:
    app: sql-orderconsumer
  ports:
  - protocol: TCP
    port: 1433
    targetPort: 1433